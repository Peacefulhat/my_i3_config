This is gentoo installation guide 2027

preparing the bootable Media -> look in gentooamd64 manual
preparing the network -> look in gentooamd64 manual

preparing the disk:
UEFI firmware with GPT disk:

partition   filesystem  Size            Description
/dev/sda1   fat32       1GB             EFI sytem partition details
/dev/sda2   linux-swap  Ram size * 1.5  Swap partition details
/dev/sda3   ext4        Remainder space Root partition details

tools:
cfdisk, fdisk: for partitioning of disk
lsblk : for look at partitions and mounted disks


formatting the disk:
formatting EFI  partition: mkfs.vfat -F 32 /dev/sda1
formatting Swap partition:
-> mkswap /dev/sda2
-> swapon /dev/sda2
formatting Root partition: mkfs.ext4 /dev/sda3

mounting the partitions:
Certain live environments may be missing the suggested mount point for Gentoo's root partition (/mnt/gentoo), or mount points for additional partitions created in the partitioning section:

-> mkdir --parents /mnt/gentoo

Continue creating additional mount points necessary for any additional (custom) partition(s) created during previous steps by using the mkdir command.

With mount points created, it is time to make the partitions accessible via mount command.

mounting root partition: mount /dev/sda3 /mnt/gentoo
mounting efi partition:
-> mkdir --parents /mnt/gentoo/efi
-> mount /dev/sda1 /mnt/gentoo/efi

installing the stage file:

choosing a stage file:
The stage file acts as the seed of a Gentoo install. Stage files are generated by the Release Engineering Team with Catalyst. Stage files are based on specific profiles, and contain an almost-complete system.

setting date and time:
-> date
-> chronyd -q
chronyd, part of net-misc/chrony can be used to update the system clock to UTC

Before downloading the stage file, the current directory should be set to the location of the mount used for the install:
-> cd /mnt/gentoo

downloading stage file with browser: download it from website and put it in /mnt/gentoo

with termianl based browser: links https://www.gentoo.org/download/
https://distfiles.gentoo.org/releases/amd64/autobuilds/
browse and download the stage file and the .asc file

its import to check wheather the thing that you download are offical if not working and you download it from offical source this step can be skipped (on your own risk).

gpg --import /usr/share/openpgp-keys/gentoo-release.asc

.asc: gpg --verify stage3-amd64-<release>-<init>.tar.xz.asc stage3-amd64-<release>-<init>.tar.xz
.DIGESTS: gpg --output stage3-amd64-<release>-<init>.tar.xz.DIGESTS.verified --verify stage3-amd64-<release>-<init>.tar.xz.DIGESTS
.sha256: gpg --output stage3-amd64-<release>-<init>.tar.xz.sha256.verified --verify stage3-amd64-<release>-<init>.tar.xz.sha256

if you want more things related to the validation and verification
you can always look into the gentoo's manual.

installing a stage file:
tar xpvf stage3-*.tar.xz --xattrs-include='*.*' --numeric-owner -C /mnt/gentoo

configuring compile options:
file path: /etc/portage/make.conf
Rustflags: RUSTFLAGS="${RUSTFLAGS} -C target-cpu=native"
MAKEOPTS:  MAKEOPTS="-j12 -l12"

chrooting:
copy DNS info:
cp --dereference /etc/resolv.conf /mnt/gentoo/etc/

Note: if using gentoo install media this can replaced as arch-chroot /mnt/gentoo

mounting the necessary filesystem:

mount --types proc /proc /mnt/gentoo/proc
mount --rbind /sys /mnt/gentoo/sys
mount --make-rslave /mnt/gentoo/sys
mount --rbind /dev /mnt/gentoo/dev
mount --make-rslave /mnt/gentoo/dev
mount --bind /run /mnt/gentoo/run
mount --make-slave /mnt/gentoo/run 

Warning:

When using non-Gentoo installation media, this might not be sufficient. Some distributions make /dev/shm a symbolic link to /run/shm/ which, after the chroot, becomes invalid. Making /dev/shm/ a proper tmpfs mount up front can fix this:

-> test -L /dev/shm && rm /dev/shm && mkdir /dev/shm
-> mount --types tmpfs --options nosuid,nodev,noexec shm /dev/shm

Also ensure that mode 1777 is set:
chmod 1777 /dev/shm /run/shm

entering the new environment:
chroot /mnt/gentoo /bin/bash
source /etc/profile
export PS1="(chroot) ${PS1}"

If the above section was skipped to use arch-chroot. Use 
arch-chroot /mnt/gentoo
export PS1="(chroot) ${PS1}"

preparing for a bootloader:
UEFI: mount /dev/sda1 /efi
DOS/Legacy Bios system: mount /dev/sda1 /boot


configuring portage:
installing a gentoo ebuild repository snapshot:
emerge-webrsync

(optional):if needed refer selecting mirrors option in the manual.
(optional): updating gentoo ebuild repo.
emerge --sync


reading news:
News items were created to provide a communication medium to push critical messages to users via the Gentoo ebuild repository. To manage them, use eselect news. The eselect application is a Gentoo-specific utility that allows for a common management interface for system administration. In this case, eselect is asked to use its news module
-> eselect news list
-> eselect news read

choosing the right profile:
A profile is a building block for any Gentoo system. Not only does it specify default values for USE, CFLAGS, and other important variables, it also locks the system to a certain range of package versions. These settings are all maintained by Gentoo's developers.

To see what profile the system is currently using:
-> eselect profile list

Optional:Adding a binary package host(refer to docs)
--getbinpkg can be passed to emerge if the host is set

some useful USE flags if no desktop is choosen:
USE="X alsa gtk dbus udisk policykit bluetooth"

updateting the world:
emerge --ask --verbose --update --deep --newuse --getbinpkg @world



time zone:
ls -l /usr/share/zoneinfo/Asia/Kolkata
ln -sf ../usr/share/zoneinfo/Asia/Kolkata

locale generation:
-> nano /etc/locale.gen
#en_US
-> locale-gen

locale selection:
eselect locale list
eselect locale set <number>


Setting the locale will avoid warnings and errors during kernel and software compilations later in the installation.

Now reload the environment:
-> env-update && source /etc/profile && export PS1="(chroot) ${PS1}"

configuring the kernel

emerge --ask sys-kernel/linux-firmware
emerge --ask sys-firmware/sof-firmware

license is needed for linux firmware(docs to setup)

file path: /etc/portage/package.use/installkernel
sys-kernel/installkernel grub

emerge --ask sys-kernel/installkernel

edit the file: /etc/portage/package.use/installkernel
sys-kernel/installkernel dracut
emerge --ask sys-kernel/installkernel

Distributed kernels

optional: signed kernel modules (refer docs)
optional: signing the kernel image(refer docs)

installing a distributed kernel:
emerge --ask sys-kernel/gentoo-kernel-bin

file path /etc/portage/make.conf
enableing: USE="dist-kernel"

emerge --ask sys-apps/pciutils

configuring the system:
emerge --ask sys-fs/genfstab
genfstab -U / >> /etc/fstab

network infomation:
hostname: echo <name> > /etc/hostname

Important
On OpenRC systems, the hostname chosen above must be configured in /etc/hosts.

An important next step may be to inform this new system about other hosts in its network environment. Network host names can be defined in the /etc/hosts file. Adding host names here will enable host name to IP addresses resolution for hosts that are not resolved by the nameserver.

nano /etc/hosts
FILE /etc/hostsFilling in the networking information

# This defines the current system and must be set
127.0.0.1     tux.homenetwork tux localhost
::1           tux.homenetwork tux localhost

# Optional definition of extra systems on the network
192.168.0.5   jenny.homenetwork jenny
192.168.0.6   benny.homenetwork benny

Save and exit the editor to continue.

DHCP:(openrc)
emerge --ask net-misc/dhcpd
rc-update add dhcpd default

networkmanager:
set the USE="${USE} networkmanager"

file path /etc/portage/package.use/networkmanager
net-wireless/wpa-supplicant dbus

emerge --ask net-misc/networkmanager
rc-update add NetworkManager default

root password:
-> passwd

init and boot configuration:(openrc)
filepath: /etc/rc.conf

keyborad configuration:
filepath: /etc/conf.d/keymaps

hwclock to update clock options
filepath: /etc/conf.d/hwclock

Systems setup from a desktop stage3 should run dbus-uuidgen at this point:
dbus-uuidgen --ensure=/etc/machine-id


installing tools:

sytemlogger:(refer docs for more infomation)
emerge --ask app-admin/sysklogd
rc-update add sysklogd default

file indexing:(optional)
emerge --ask sys-apps/mlocate

remote shell access:
rc-update add sshd default

time synchronization:
emerge --ask net-misc/chrony
rc-update add chronyd default

configuring the bootloader:
echo 'GRUB_PLATFORMS="efi-64"' >> /etc/portage/make.conf
emerge --ask sys-boot/grub
emerge --ask --update --newuse --verbose sys-boot/grub

intall:

grub-install /dev/sda
for UEFI systems: grub-install --efi-directory=/efi

configure:
grub-mkconfig -o /boot/grub/grub.cfg

Finishing:

First install app-portage/gentoolkit:
-> emerge --ask app-portage/gentoolkit

For cleaning old source tarballs, use:
-> eclean-dist

For cleaning old binpkgs, use:
-> eclean-pkg

User administration
Adding a user for daily use

Working as root on a Unix/Linux system is dangerous and should be avoided as much as possible. Therefore it is strongly recommended to add one or more standard user account(s) for day-to-day use.

The groups the user is member of define what activities the user can perform. The following table lists a number of important groups:
Group 	Description
audio: 	Enable the user account to access the audio devices.
cdrom: 	Enable the user account to directly access optical devices.
cron: 	Enable the user account to access time-based job scheduling via cron. Note: user accounts on systems running the systemd service system can use systemd timers and user service files instead of cron jobs.
floppy: 	Enable the user account to directly access ancient mechanical devices known as floppy drives. This group is not generally used on modern systems.
usb: 	Enable the user account able to access USB devices.
video: 	Enables the user account to access video capturing hardware and hardware acceleration.
wheel: 	Enables the user account able to use the su (substitute user) command, which allows switching to the root account or other accounts. For single user systems that include a root account, it is a good idea to add this group for the primary standard user.


adding user:
useradd -m -G wheel,audio,video -s /bin/<shellname> <username>
passwd <usernamel>

disk cleanup:
rm stage3-*.tar.xz


gentoo sddm setup:

emerge --ask gui-libs/display-manager-init

The configuration file should be modified to use SDDM:
FILE /etc/conf.d/display-managerSet SDDM as the display manager

CHECKVT=7
DISPLAYMANAGER="sddm"

emerge --ask elogind

rc-update add elogind boot
rc-update add displaymanager default

usermod -a -G video sddm

gentoo i3 setup:

emerge --ask x11-wm/i3
emerge --ask x11-misc/i3blocks

starting i3:

It's recommended that Dbus is used for applications to work correctly.
Most users will probably want i3 started via the X server's ~/.xinitrc file, probably replacing the contents of any existing ~/.xinitrc with the following:

FILE ~/.xinitrc
#!/bin/sh
# Option 1: launch with a dbus session (recommended)
exec dbus-launch --exit-with-session i3

gentoo emacs:

/etc/portage/package.use/emacs
app-editors/emacs gui jpeg png tiff webp xpm

emerge --ask emacs

emerge --ask ffmpeg

gentoo audio:(pipewire)

update USE flag (make.conf)
USE="bluetooth elogind gstreamer pulseaudio ffmpeg"

local use flag

/etc/portage/package.use/pipewire

media-video/pipewire pipewire-alsa sound-server extra jack-client jack-sdk

emerge --ask media-libs/libpulse

audio groups:
-> usermod -aG pipewire <username>
remove from audio group
-> usermod -rG audio <username>
-> emerge --ask sys-auth/rtkit

 pw-config list file locations

enbaling:
rc-update add -U pipewire default
rc-update add -U pipewire-pulse default 
 
To start the services without enabling them:
rc-service --user pipewire start
rc-service --user pipewire-pulse start 

To confirm PulseAudio server emulation:
LANG=C pactl info | grep "Server Name"

Server Name: PulseAudio (on PipeWire 0.3.39)
